{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build && mv .nuxt ../ && mv .netlify ../",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.4",
    "nuxt-og-image": "^2.1.1",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5"
  }
}
